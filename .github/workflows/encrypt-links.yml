name: Encrypt quick links

on:
  push:
    branches: [main]
    paths:
      - "links/index.html"
      - "scripts/encrypt-links.mjs"
      - ".github/workflows/encrypt-links.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  encrypt:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - id: encrypt
        name: Encrypt vault
        env:
          HIDE_LINK: ${{ secrets.HIDE_LINK }}
          QUICK_LINKS_JSON: ${{ secrets.QUICK_LINKS_JSON }}
          QUICK_LINKS_ITERATIONS: "600000"
        run: |
          if [ -z "$HIDE_LINK" ]; then
            echo "GitHub Secret HIDE_LINK is required as the unlock key."
            exit 1
          fi
          if [ -z "$QUICK_LINKS_JSON" ]; then
            echo "GitHub Secret QUICK_LINKS_JSON is required as the encrypted source."
            exit 1
          fi
          node scripts/encrypt-links.mjs links/vault.json
          echo "vault_ready=true" >> "$GITHUB_OUTPUT"

      - name: Commit encrypted vault
        if: steps.encrypt.outputs.vault_ready == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add links/vault.json
          if git diff --cached --quiet; then
            echo "No encrypted vault changes."
            exit 0
          fi
          git commit -m "Update encrypted quick links"
          git push
